-provide(:title, "Find back password")

.jumbotron
  %h2 Reset your password 
  =form_for :password, url: password_path(:password) do |password_form|
    .form-group
      =password_form.text_field :email, {class:"form-control", placeholder: "Email for reseting your password"}

    =password_form.submit "Send", {class:"btn btn-primary btn-lg btn-block"}
